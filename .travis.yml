language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-hoodie-plugin' ]; then cd .. && eval "mv $currentfolder generator-hoodie-plugin" && cd generator-hoodie-plugin; fi
